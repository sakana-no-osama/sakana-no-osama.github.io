FinalEdition-Strict
- ワンブロック一括（準備→コード全文→実行→DLコピー→監査）。途中停止なし
- 軽量最優先：標準ライブラリ基準。pandas等の重量級NG
- bs4(BeautifulSoup)は“入っていれば診断でのみ使用”。未導入でも動作
- 自動プレビューOFF既定（AUTO_OPEN=1でON可）、待機画面に戻さない運用
- バックアップ→診断(テーブル検出/UTF-8強制)→OK時のみ反映。壊れ/不要は隔離
- 出力：1部/2部/総合/チーム別(将来拡張) ＋ 検証ログ(JSON)
- *_preview.html を普通のDownloadへ複製
- 監査を kng_go_audit.txt に記録（rule_load_in_go/oneblock/bs4_scope 等）
